import "./App.css";

function App()
{
  return (
    <div className={"root"}>
        <header className={"a"}>
            ma powiedzieć gdzie jesteśmy (konkretne koordymaty).
        </header>

        <section className={"b"}>
            b
        </section>

        <section className={"c"}>
            c
        </section>

        <aside className={"d"}>
            d
        </aside>

        <article className={"e"}>
            e
        </article>

        <footer className={"f"}>
            f
        </footer>
    </div>
  );
}

export default App;
